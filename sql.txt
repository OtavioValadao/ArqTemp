create database locacao; #cria o banco de dados

create table clientes(  #cria a tabela do banco
cpf varchar(100), #cria campos dentro da tabala
telefone varchar(100),
endereco varchar(100),
email varchar(100)
);

select * from clientes; #seleciona a tabela para mostrar informaçoes 